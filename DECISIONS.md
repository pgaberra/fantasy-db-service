# Decision log

Non-obvious choices in this service, one line each. Format and rules: the monorepo root `DECISIONS.md`.

---

[2026-09-11] fantasy-db-service: an unknown path answers 404 through its own `NoResourceFoundException` handler, copied from bff and espn-service (fantasy-workspace#33) — a shared advice library was rejected because the services deploy separately and it would couple their releases for about ten lines, and a catch-all that passes every Spring `ErrorResponse` 4xx through quietly was left for a separate decision across all four services rather than widening this fix.
[2026-09-11] fantasy-db-service: a premium grant truncates its instants to microseconds in the entity, so it holds exactly what `TIMESTAMPTZ` stores (Postgres and H2 both round half up, so an instant ending in ≥500 ns was stored after itself) — rejected: an injected `Clock` ticking in microseconds, which covers only the instants the service stamps and not a caller's `expiresAt`; and changing every entity, since grants are the only rows queried against a freshly stamped `now`.
[2026-09-11] fantasy-db-service: an event about a different subscription takes over a user's one subscription row only if the stored one has ended, or is live but grants no premium while the new one does; two live ones at once log ERROR — the row used to follow whichever event came last, so canceling one of two subscriptions took premium from a user still paying for the other; a row per subscription was rejected because every reader (entitlements, the portal, the admin list) wants the one subscription that counts, and keeping one row keeps that answer in one place.
[2026-09-11] fantasy-db-service: a user's open checkout is stored in its own `pending_checkouts` table, one row per user, replaced only by compare-and-set on the reference the caller read — so two tabs opening checkout at once end up on the same provider transaction, which can be paid once; a row on `subscriptions` was rejected because a checkout is not a subscription until it is paid, and keying the reuse on the Paddle customer was rejected because accounts with an unverified email have none.
[2026-09-11] fantasy-db-service: local `docker-compose.yml` runs `postgres:16-alpine`, the major the staging and production Coolify resources run (checked 2026-09-11), instead of 17 (#113) — local Postgres is only worth having if it matches the servers, since tests run on H2 and a migration is first exercised on the staging deploy; upgrading the servers to 17 was left as Alexander's call rather than decided in a docs fix.
[2026-09-13] fantasy-db-service: the player id remap removes an uncovered row whose old id is another player's destination in the crosswalk, and refuses an apply over such a draft pick (fantasy-workspace#52: Yahoo's 5738 Frk would have sat under ESPN's 5738 Dumoulin in all 7 prod projections) — keeping it broke the "invisible and recoverable" premise the keep-never-drop rule rests on; moving leftovers to negative ids was rejected because every save path would then have to accept ids no pool has, and a collision with an ESPN id outside the crosswalk is not caught here since this service never sees the pool (checked by hand for prod: none).
[2026-09-15] fantasy-db-service: the player id remap takes `from`/`to` (default yahoo→espn) so the pool can move back to Yahoo, a share is stamped with its projection's id space (V23 corrects the existing ones), the projection response carries `playerIdSpace`, and an update that says it was built from another space than the stored one is refused with 409 — every share had taken the column default `yahoo`, so staging's three ESPN-id shares would have been translated as Yahoo ids; a second endpoint for the reverse direction was rejected as a copy of the same rules, and checking the space in the BFF alone was rejected because the invariant belongs where the rows are written, which is the one place a save cannot go around.
[2026-09-16] fantasy-db-service: linking Google or Facebook to an account whose email is not yet verified drops the account's password and bumps `tokenVersion`; a verified account keeps its password — the password on an unverified account was set by whoever registered the address first, so keeping it let an attacker pre-register a victim's email and stay logged in after the victim signed in with the provider; refusing to link until the email is verified was rejected because it would lock out every real user who registered, never clicked the link, and then chose "Sign in with Google", and that user can keep signing in with the provider (password-less accounts have no password to reset).
[2026-09-16] fantasy-db-service: the container runs as `app` with a fixed UID/GID of 10001, given numerically to `USER`, instead of root — the base image's existing `ubuntu` user (1000) was rejected as an account the image's maintainers own and may change, and handing the user a writable `/app` was rejected because the service writes only to `java.io.tmpdir` (/tmp), so the unpacked jars stay root-owned and read-only to it.
[2026-09-17] fantasy-db-service: V24 deletes preset drafts that never got a draft (`data.draft` absent) and are over an hour old, once, rather than a scheduled cleanup — the web stopped creating such boards (fantasy-web creates a preset draft only when its setup is confirmed), so the leftovers are a fixed set; the hour spares a setup open in a tab still on the old web, which saves into its board on confirm.
[2026-09-17] fantasy-db-service: a draft holds its own league as `DraftState.settings` (a new `DraftSettings`, the league half of `ProjectionSettings`) rather than reusing `ProjectionSettings`, whose scale and decimal settings describe the numbers and mean nothing to a draft; Alexander decided a draft's league must never write into the projection it is played against. The goalie minimum cap is 84 in both, a full NHL season, where `ProjectionSettings` had said 82 while the web allowed 84.
[2026-09-17] fantasy-db-service: a published share keeps `manualRanking` while it still strips the sync details, the player basis and the unacknowledged notice (#131) — the page was ordered by that ranking, so an import that recomputed from stats would rank differently from the page it was copied from, which is the reason the author's position overrides already travel; stripping it as "the owner's own" was rejected because a hand ranking is the board, not the owner's private league details.
[2026-09-17] fantasy-db-service: `POST` create numbers a name the user already holds (`"Apples & Ginos (2)"`) through the same `freeNameFrom` a share import uses, instead of refusing it with a 409 — a create arrives with the work already done (a spreadsheet parsed, a board built), so refusing it throws that away over something the owner can rename in a second; a `numberNameIfTaken` flag on the request was built first and dropped on Alexander's call that the new-projection page should behave the same way, and a rename still 409s because there the name is the whole of what was asked for.
[2026-09-17] fantasy-db-service: a share's author picture is served by token (`GET /api/v1/shares/{token}/avatar`) and the snapshot carries only its stamp, so the BFF still never receives a user id for a public page and half a megabyte of image bytes stays out of every page load — returning the bytes inline on the snapshot was rejected for that weight and because it could not then be cached, and reusing `GET /api/v1/users/{userId}/avatar` was rejected because it would first have to hand the share's user id to the caller.
[2026-09-19] fantasy-db-service: an import stamped with a share token is a **follow** of that link (V25) — it mirrors the author's board on every publish, takes nothing from an update but `data.draft`, is unique per (user, token), sits outside the user's name namespace and is deleted with the share by a cascading foreign key; a reader who wants their own board takes a copy (`POST /projections/copies`), which is stamped with nothing. Rejected: keeping the frozen copy and adding a "refresh" button, because the copy then silently disagrees with the page it was taken from and nobody knows which numbers they are drafting against; and letting the BFF decide what a follow may change, because every save goes through the same PUT and the one place a save cannot go around is here.
[2026-09-19] fantasy-db-service: an update to a follow **ignores** everything but the draft rather than refusing the request with a 400 — draft mode and the clear-draft path send the name and settings back exactly as they loaded them, so a publish landing between the load and the save would otherwise turn a pick into an error, and the BFF re-saves a reconciliation straight after an import; refusing was written first and dropped for that reason. The cost is that the BFF's pool reconciliation no longer persists on a follow and has to be done per read (bff follow-up).
[2026-09-19] fantasy-db-service: following a link twice returns the follow the user has, with **200** rather than 201, and `POST .../imports` no longer takes a `name` — a follow is named by its author, so there is nothing for the follower to choose and nothing of theirs to clash with; numbering a second follow was rejected because two rows mirroring one link are two copies of one board that nothing in the list tells apart.
[2026-09-19] fantasy-db-service: V25 **deletes** an import whose share is gone (Alexander's call) and demotes a duplicate or self-import to a `PROJECTION` with the origin columns cleared, rather than deleting those — a duplicate may hold a draft the kept follow does not; the delete loops, because deleting a row cascades away any share of it and can strand a follow of *that* link in turn.
